# v1.0 launch checklist

**Final state of the public launch, 2026-09-23. Every item is done.** Each row names the
evidence it was checked against.

## Content and quickstart

| Item | Status | Evidence |
|---|---|---|
| README understandable in <30 s | ✅ done | `README.md` opens with the positioning line and the throughput figure (`docs/readme/hero-throughput.svg`), then goes through why, install, quickstart, routing, serving, correctness, platforms, reproduction, contributing and limitations. Deep material is in `docs/` |
| Installation from a clean environment | ✅ done | The install matrix (Python 3.11, 3.12, 3.13 × base and `ane` extras, each in a fresh venv) passes in `benchmarks/release-gate-1.0.0.md`. Installing from PyPI, `pip install laya-apple==1.0.1` and `==1.0.2`, was verified in fresh venvs after each upload |
| Quickstart works | ✅ done | The install matrix runs the README quickstart snippet in each environment (`scripts/release_gate.py`, `_quickstart_snippet`) |
| Examples work | ✅ done | `examples/basic.py`, `auto_routing.py` and `heterogeneous_serving.py` each exit 0 against the cached checkpoints on the tested Mac. `auto_routing.py` routes the short single question to the ANE, and the long and multi-question requests to MLX |
| Benchmark command works | ✅ done | `scripts/hardware_report.py --quick` produced the first community matrix entry, `hardware-results/apple-m4-max-macos26/` |
| Raw data available | ✅ done | `benchmarks/v1.0/` holds the raw data behind every table in `benchmarks/v1.0.md`. `research/` holds the Phase -1 and v0.2 research data |
| Headline numbers trace to raw data | ✅ done | The README figures are generated from `benchmarks/v1.0/` and `routing.json` by `scripts/generate_readme_svgs.py`, and CI fails on drift. `docs/reproducibility.md` maps each headline number to its report, raw file and command |

## Legal, process and repository setup

| Item | Status | Evidence |
|---|---|---|
| License / NOTICE | ✅ done | `LICENSE` (Apache-2.0) and `NOTICE` at the repo root, and in both the wheel and the sdist |
| SECURITY.md, CODE_OF_CONDUCT.md | ✅ done | At the repo root. Private reporting goes through GitHub security advisories |
| CONTRIBUTING.md | ✅ done | Covers setup, test tiers, parity, benchmarks, hardware results, backends, the protected-`main` workflow, the release gate and publishing |
| Agent guidelines | ✅ done | `AGENTS.md` holds the canonical rules for coding agents. `CLAUDE.md` imports it |
| Issue / PR templates | ✅ done | `.github/ISSUE_TEMPLATE/` and `.github/pull_request_template.md` |
| Private content removed before publication | ✅ done | History was rewritten once, before the first push, to remove a private planning document and local paths. See the note in `CHANGELOG.md`. Published history is never rewritten again |
| Repository public | ✅ done | `tc3oliver/laya-apple` is public |
| Description / topics | ✅ done | Applied with `scripts/github_seed.sh --apply`: the description plus 9 topics |
| Labels and seeded issues | ✅ done | 14 labels from `.github/labels.yml`, and 18 issues (#1–#18) from `.github/issue-drafts/`, labelled `good first issue`, `help wanted` and `research` |
| Social preview | ✅ done | `docs/readme/social-preview.png`, generated by `scripts/generate_readme_svgs.py --social-png` and uploaded in the repository settings (GitHub has no API for it) |
| `main` protection | ✅ done | Ruleset `main`: pull request required, squash merge only, the `test` check required, force pushes and deletion blocked, no bypass |

## Releases

| Item | Status | Evidence |
|---|---|---|
| Tags | ✅ done | `v0.1.0`, `v0.2.0`, `v0.3.0`, `v1.0.0`, `v1.0.1` and `v1.0.2` are on `origin`. `v1.0.2` is on its release merge commit on `main` |
| Release notes / GitHub releases | ✅ done | 1.0.0 (from `docs/releases/v1.0.0.md`), 1.0.1 and 1.0.2 (from `CHANGELOG.md`). 1.0.2 is Latest |
| PyPI | ✅ done | [`laya-apple`](https://pypi.org/project/laya-apple/) 1.0.1 and 1.0.2 were published by `.github/workflows/release.yml` with Trusted Publishing (GitHub OIDC). There is no API token. The PyPI page shows the current README. Procedure: [`publishing.md`](publishing.md) |
| Known limitations | ✅ done | The README's "Limitations" section is published and kept. `AGENTS.md` forbids removing a limitation without new measurements |
